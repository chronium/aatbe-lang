use "libc"

@entry
fn main () -> () = {
    val len: i32 = strlen "Hello World\n"
    var hw: str = (malloc len) as str
    strncpy (hw) ("Hello World\n") (len)
    printf hw
    hw[0] = 'J'
    printf hw
}
